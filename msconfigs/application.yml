spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  cloud:
    stream:
      rabbit:
        bindings:
          springCloudBusInput:
            consumer:
              exclusive: false
management:
  endpoints:
    web:
      exposure:
        include: "bus-refresh"
